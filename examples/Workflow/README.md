# Workflow examples

These YAML files are alternative workflow configurations for the same example
event. Run them individually with `mtuq-run`; this directory is not a catalog.

From a source checkout, unpack the example data first:

```bash
bash data/examples/unpack.bash
```

The recipes use paths relative to this directory and therefore expect the
repository's `data/examples/` directory to be present.
